{% extends 'base.html.twig' %}

{% block title %}{{ 'Ajouter une critique sur le film ' ~ movie.title }}{% endblock %}

{% block body %}
  <div class="my-5 py-5 text-center">
    <h1 class="display-6 fw-bold">{{'Ajouter une critique sur le film ' ~ movie.title}}</h1>
  </div>
  <div class="container w-50">
    {{ form(form, {attr: {novalidate: 'novalidate'}}) }}
  </div>
{% endblock %}
