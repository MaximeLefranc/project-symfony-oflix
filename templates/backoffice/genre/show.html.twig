{% extends 'backoffice/backofficebase.html.twig' %}

{% block title %}Genre
{% endblock %}

{% block body %}
	<h1>Genre</h1>
	<a class="btn btn-info" href="{{ path('backoffice_genre_index') }}">back to list</a>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ genre.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ genre.name }}</td>
			</tr>
		</tbody>
	</table>


	<a class="btn btn-warning" href="{{ path('backoffice_genre_edit', {'id': genre.id}) }}">edit</a>

	{{ include('backoffice/genre/_delete_form.html.twig') }}
{% endblock %}
