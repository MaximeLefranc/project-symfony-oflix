{% extends 'backoffice/backofficebase.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>
    <a class="btn btn-info" href="{{ path('backoffice_user_index') }}">back to list</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-info" href="{{ path('backoffice_user_edit', {'id': user.id}) }}">edit</a>

    {{ include('backoffice/user/_delete_form.html.twig') }}
{% endblock %}
